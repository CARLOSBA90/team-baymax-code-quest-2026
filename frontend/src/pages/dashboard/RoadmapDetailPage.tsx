/**
 * Detalle de una ruta (`/dashboard/roadmaps/:roadmapId`). Placeholder vacío dentro del
 * `DashboardLayout` hasta que exista la vista de detalle.
 */
export const RoadmapDetailPage = () => <section aria-label="Detalle de la ruta" />;
